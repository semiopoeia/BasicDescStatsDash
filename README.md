This code should be flexible for any data set you might feed into it, the comments show that all one might have to do is simply specify the filepath for your data and the rest needs no additonal specifications, unless one hopes for further customization. It can use but does not require various grouping and subsetting variables (and the option to specify a condtioning range on a single scale variable) there are some issues (namely, display issues) that get augmented with several grouping options specified
